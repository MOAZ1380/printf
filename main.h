#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int _printf(const char *format, ...);
int _putchar(char c);
int _printstr(char *str);
int _printint(int num);

#endif /* MAIN_H */
